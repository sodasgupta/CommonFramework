cucumber:
   reanmigrate:
       automation:
         authentication:
            admin_password: "${admin_password:admin}"
            admin_user: "${admin_user:admin}"
            ssh_key: "${SSHKEY:}"
     
         common:
            browser: "${BROWSER:Chrome}"
            platform_url: "${URL:https://160.1.49.15/#/user/login}"        
    