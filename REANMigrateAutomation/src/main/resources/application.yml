cucumber:
   reanmigrate:
       automation:
         authentication:
            admin_password: "${admin_password:admin}"
            admin_user: "${admin_user:admin}"
            ssh_key: "${SSHKEY:}"
     
         common:
            browser: "${BROWSER:Chrome}"
            platform_url: "${URL:http://50b1f6f7.ngrok.io/SpringBootCRUDApp/}"        
    